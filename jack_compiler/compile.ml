(** Contains compiler logic for transforming Jack AST to VM code. *)

(** Compiles Jack code to VM code as a list of strings. *)
let compile (_jack_code : Ast_types.class_declaration) : string list =
  [ "TODO" ]
